-- Demo seed data for PostgreSQL (used by docker-compose init)
-- Note: Python seed.py handles full seeding for the API; this provides baseline reference data.

-- This file is loaded after schema creation by the API on startup.
-- For Docker PostgreSQL init, run the API once to seed via seed.py.

SELECT 1;
